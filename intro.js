/**
 * Node.js is basically a server side javaScript.
 * Node get it's javaScript support from V8 virtual Machine (built with C++)
 * that runs in the browser.
 * 
 * A wrapper around a virtual machine V8
 */